import booksSlice, {
  setFoundBooks,
  addMoreBooks,
  resetBooks,
} from "./book-card/model/slice";
import BookCard from "./book-card/ui/book-card";

export { booksSlice, BookCard, setFoundBooks, addMoreBooks, resetBooks };
